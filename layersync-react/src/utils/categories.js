// Canonical colors for change categories. Used by annotation dots, canvas
// highlight bands, and the change-log rows so a category reads as the same
// color everywhere. Keep this the single source of truth.
export const CATEGORY_COLORS = {
  Visual:     '#3B82F6',
  Layout:     '#8B5CF6',
  Typography: '#F59E0B',
  Color:      '#14B8A6',
}
